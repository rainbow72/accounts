<header class="mb-4">
  <nav class="navbar navbar-expand-sm navbar-dark bg-primary">
    <div class="center">
      <a class="navbar-brand" href="/">Accounts Book</a>
    </div>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <li class="nav-item"><%= link_to "Signup", root_url, class: "nav-link" %></li>
<%#
        <% if logged_in? %>
          <li class="nav-item"><%= link_to "Logout", root_url, class: "nav-link" %></li>
<%#
        <% else %>
          <li class="nav-item"><%= link_to "Login", root_url, class: "nav-link" %></li>
<%#
        <% end %>
      </ul>
    </div>
  </nav>
</header>
